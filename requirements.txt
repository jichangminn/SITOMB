# Buka file requirements.txt dan pastikan isinya hanya
# berisi nama-nama paket seperti di bawah ini:
echo -e "streamlit\nstreamlit_option_menu" > requirements.txt
# Commit dan push perubahan ke GitHub
git add requirements.txt
git commit -m "Fix requirements.txt syntax"
git push
